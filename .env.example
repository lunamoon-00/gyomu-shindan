# ===========================================
# 業務効率化診断ツール 環境変数サンプル
# コピーして .env.local にリネームし、値を設定してください
# ===========================================

# GAS Web App のデプロイURL（診断・相談・Slack通知のエンドポイント）
# 必須（本番時）。以下はサンプル、実際のGASデプロイURLに置き換えてください
GAS_URL=

# 相談リクエストの送信先メールアドレス
# クライアントで参照するため NEXT_PUBLIC_ が必要
NEXT_PUBLIC_CONSULT_EMAIL=

# 開発時のモック切り替え（true で診断APIをモックに、本番は false）
NEXT_PUBLIC_USE_MOCK=false
